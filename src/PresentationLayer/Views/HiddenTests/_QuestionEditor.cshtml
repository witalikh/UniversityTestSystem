@using DataAccessLayer.Enums
@model BusinessLayer.ViewModels.QuestionViewModel

<div class="form-group">
    @Html.HiddenFor(m => m.Id)
    @Html.HiddenFor(m => m.TestId)
    @Html.HiddenFor(m => m.InnerOrder)
    
    @Html.TextAreaFor(m => m.QuestionText, new { @class = "form-control" })
    @Html.DropDownListFor(m => m.Type, new SelectList(Enum.GetValues(typeof(QuestionType))), "-- Select --", new { @class = "form-control" })
    @Html.TextBoxFor(m => m.Marks, new { @class = "form-control" })


    @*<div class="row">
        <div class="col-md-8">
            @Html.LabelFor(m => m.QuestionText)
            
        </div>
        <div class="col-md-4">
            <div class="form-group">
                @Html.LabelFor(m => m.Type)
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Marks)
            </div>
        </div>
    </div>

    <div id="choice-editor-container">
        @if (Model.Type == QuestionType.SingleChoice || Model.Type == QuestionType.MultipleChoice)
        {
            await Html.PartialAsync("_QuestionChoiceEditorContainer", Model.ChoiceOptions.ToList());
        }
    </div>*@
</div>
